{
  "name": "tka-toni-v2",
  "version": "1.0.0",
  "description": "TKA Toni - Exam Platform built with React, TypeScript, and Supabase",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "vite --open",
    "build": "vite build",
    "lint": "vite run lint",
    "type-check": "vite  run type-check",
    "clean": "vite run clean && rm -rf node_modules dist"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "~5.9.3"
  }
}
